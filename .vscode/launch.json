// .vscode/launch.json
{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Debug path",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/path",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build"
        }
    ]
}