[package]
name = "path"
version = "0.1.0"
authors = ["compcy <your_email@example.com>"]
edition = "2021"

[dependencies]
clap = "^2.33"  # Command line argument parser

[dev-dependencies]
assert_cmd = "2.0"
predicates = "2.1"
tempfile = "3.5"

[profile.release]
opt-level = 3
